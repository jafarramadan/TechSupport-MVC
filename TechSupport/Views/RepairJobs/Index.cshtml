@model IEnumerable<TechSupport.Models.RepairJob>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JobDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Completed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScheduledDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JobDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Completed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScheduledDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RepairJobId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RepairJobId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RepairJobId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
